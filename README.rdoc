The database here is incomplete. The document 'links_to_be_included.docx' lists all the currently known required links; this will change as the network firms up.

Brief interface demo for CAW Waverley tender request.

Requirements:
PostgreSQL
Ruby 2.0
Rails 4.2.0

Setup:
~$rake db:create
~$rake db:migrate
~$rake db:seed

Run:
~$rails s

localhost:3000

